spring:
  application:
    name: intellirecruit
  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY}
        chat:
          options:
            model: gemini-2.5-flash   # free tier, fast
            temperature: 0.7
            max-output-tokens: 2048
    retry:
      max-attempts: 1
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT}
      password: ${REDIS_PASSWORD}
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${GMAIL}
    password: ${GMAIL_APP_PASSWORD}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  kafka:
    bootstrap-servers: ${KAFKA_HOST}:${KAFKA_PORT}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
    consumer:
      group-id: intellirecruit-group
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring:
          json:
            trusted:
              packages: com.smartstaff.intellirecruit.kafka.event

#app:
#  jwt:
#    secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
#    expiration: 86400000   # 24 hours in milliseconds

# ---------- App-level config -------------------------
app:
  redis:
    ai-cache-ttl: 3600    # cache AI responses for 1 hour (seconds)
  kafka:
    topics:
      ai-events: ai-generated-events
      email-events: email-notification-events